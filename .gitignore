# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# IDE
/.idea/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
.env.local
.env.*.local

# supabase
supabase/.branches
supabase/.temp

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/tests/.auth/

# clerk configuration (can include secrets)
/.clerk/

# ─── SECURITY: never commit these ────────────────────────────────────────────
# Internal audit + operations docs. The QA report enumerates unfixed exploitable
# vulnerabilities with working repro commands and the production project ref.
# This must never reach a public repo, and it is the reason this repo is private.
# Scoped to .md on purpose: a bare *REMEDIATION* also matched
# lib/__tests__/remediation-invariants.test.ts (git uses core.ignorecase on macOS),
# silently dropping a real test file from the repo.
#
# `/docs/*` (not `/docs/`) so the allowlist below can work at all: a gitignore
# negation cannot re-include a file whose PARENT DIRECTORY is excluded. Everything
# in docs/ stays ignored by default; the runbooks below are deliberate exceptions
# and are operational procedure, not vulnerability disclosure. Adding a new
# committed doc means adding a line here — `git add -f` works but leaves the next
# person's `git status` lying to them.
/docs/*
!/docs/enterprise-sso-runbook.md
!/docs/email-deliverability.md
# Committed on purpose: procurement teams ask for the audit behind the public
# conformance statement, and a statement with no auditable record behind it is
# a claim, not evidence. It lists accessibility gaps, not security ones.
!/docs/accessibility-audit.md
# The handoff set. These are the succession plan: what to buy, how to launch,
# how to operate, and how to hand the project to whoever comes next. They were
# written 2026-08-25 and sat UNTRACKED for a full session because `/docs/*`
# swallowed them and `git add -A` reports success while skipping ignored paths —
# exactly the "status is lying to you" failure this block warns about.
#
# They contain procedure, public DNS values and env var NAMES. No secrets, no
# vulnerability disclosure. docs/verification-backlog.md is deliberately NOT
# here: it enumerates past P0s and belongs on the ignored side of the line.
!/docs/LAUNCH-CHECKLIST.md
!/docs/GO-LIVE-CUTOVER.md
!/docs/RUNBOOK.md
!/docs/PURCHASE-CHECKLIST.md
*QA-REPORT*.md
*QA-FINDINGS*.md
*REMEDIATION*.md
*HANDOFF*.md

# secrets, keys, credentials
*.key
*.p12
*.pfx
*.crt
*.jks
*.keystore
# Root-anchored on purpose: a bare `credentials.*` also matches the real source
# file app/actions/credentials.ts. It survives today only because it was tracked
# before this pattern existed — untrack it once and it silently disappears.
/secrets.*
/credentials.*
/service-account*.json
*.jwt
*.sid

# local scratch / agent working files
/scratchpad/
/.scratch/
*.local.md
.claude/settings.local.json

# database dumps + backups (contain real user data)
*.dump
*.sql.gz
*.sqlite
*.sqlite3
/backups/
/supabase/.dump/

# uploaded artifacts (coach photo IDs, team media)
/uploads/
/tmp/

# Gemini audit pack output. Same rationale as the QA-REPORT / REMEDIATION patterns
# above: these enumerate still-unfixed exploitable findings with working repro steps
# and file:line locations. The audit *prompts* in prompts/audits/prompts/ are
# committed on purpose — they are instructions, not disclosure. The evidence and the
# fix packs are not. `.gitkeep` stays so the directories survive a fresh clone.
/prompts/audits/findings/*
!/prompts/audits/findings/.gitkeep
/prompts/audits/handoff/*
!/prompts/audits/handoff/.gitkeep

# agent working files
/scratch/
/dev.log
/dev.pid
/server.log
/server.pid
/build_output.log
/run-tests.sh
/scratch.md
